{
  "name": "@pdfcraft/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and schemas for pdfcraft monorepo",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@pdfcraft/config": "workspace:*",
    "@types/node": "^20.10.4",
    "typescript": "^5.3.3"
  }
}
